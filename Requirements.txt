# This is contains the required libraries to be loaded
# Core dependencies
python-dotenv>=1.0.0
requests>=2.31.0
Pillow>=10.0.0
paho-mqtt>=1.6.1

# Waveshare e-paper display
# Install from: https://github.com/waveshare/e-Paper
# Or: pip install waveshare-epd
waveshare-epd>=0.2.0

# GPIO control for Raspberry Pi
pigpio>=1.78

# System monitoring (optional but recommended)
psutil>=5.9.0

# HTTP retry logic (included with requests, but explicit for clarity)
urllib3>=2.0.0
